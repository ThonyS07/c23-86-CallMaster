//? class en el div del dashboar <div className="h-full bg-background1 rounded-lg dark:bg-gray-800 shadow-lg p-4" >
/*layout
    const pathname = usePathname();
    const isLoginPage = pathname === "/login";
    return (
        <html lang='en'>
            <body
                // className={`${montserratFont.variable} ${robotoFont.variable} antialiased h-full w-full flex`}>
                className={`${montserratFont.variable} ${robotoFont.variable} `}>
                {/* className={`${montserratFont.variable} ${robotoFont.variable} antialiased flex flex-grow h-screen w-full bg-gradient-to-r from-blue-500 via-indigo-600 to-purple-700 dark:bg-background3 overflow-hidden`}> }
                <Providers>
                    <div className='antialiased grid grid-cols-[auto_1fr] h-screen w-full bg-gradient-to-r from-blue-500 via-indigo-600 to-purple-700 dark:bg-background3 overflow-hidden'>
                        <div>{!isLoginPage && <Sidebar />}</div>

                        <div>{children}</div>
                    </div>
                </Providers>
            </body>
        </html>
    );

Page.tsx

// <div className='flex-grow w-full h-full bg-white shadow-lg rounded-lg overflow-auto'>
		<section className='flex flex-grow  min-h-screen shadow-lg rounded-lg text-primary3 dark:bg-background3 dark:text-primary2  '>
			<div className='w-1/5 flex flex-col px-6 py-4 overflow-auto'>
				<Notifications />
			</div>

            	<div className='w-4/5 flex flex-col px-6 py-4 overflow-auto '>  TITULO

                <div className='bg-background1 rounded-lg shadow-lg p-4'>
					<h1 className='text-3xl bg-backgorund1 font-bold text-primary1 dark:text-primary2 mb-6'>
						Dashboard y Reportes
					</h1>
					{/* Sección de Estadísticas }
					<StatisticsSummary stats={stats} onCardClick={handleCardClick} />


                    	{showNotifications && selectedState && recentTickets[selectedState] && (
						<div className='mt-6'>
							<h2 className='text-xl font-semibold mb-4 text-accent1 dark:text-accent3'>
								Tickets: {selectedState}
							</h2>
							<TicketsTable tickets={recentTickets[selectedState]} />
						</div>
					)}
				</div>

                	<div className='w-full flex-grow bg-background1 rounded-lg shadow-lg p-4'>
					<h1 className='text-2xl font-bold mb-4 text-primary1 dark:text-primary2'>
						Reportes


                        	<Table
						// columns={columnas}
						data={users2}
						initialVisibleColumns={[
							"id",
							"name",
							"age",
							"role",
							"team",
							"email",
							"status",
						]}
						statuses={[
							{ uid: "active", name: "Active" },
							{ uid: "paused", name: "Paused" },
							{ uid: "vacation", name: "Vacation" },
						]}
						viewClick={handleViewClick}
					/>



*/

     
